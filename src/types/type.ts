export type Workspace = {}

export type Article = {}

export type UserRegisterDTO = {
    fullname: string;
    email: string;
    password: string;
}

export type UserLoginDTO = {
    email: string;
    password: string;
}